import { Icon } from 'lucide-react';
import { cowHead, cowUdderDroplets } from '@lucide/lab';

const App = () => {
  return (
    <div>
      <Icon iconNode={cowHead} />
      <Icon iconNode={cowUdderDroplets} />
    </div>
  );
};

export default App;