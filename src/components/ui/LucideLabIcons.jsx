// src/components/LucideLabIcons.jsx
import { Icon } from 'lucide-react';
import { cowHead, cowUdderDroplets } from '@lucide/lab';

export const CowHeadIcon = (props) => (
  <Icon iconNode={cowHead} {...props} />
);

export const CowUdderIcon = (props) => (
  <Icon iconNode={cowUdderDroplets} {...props} />
);